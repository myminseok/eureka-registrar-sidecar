name: eureka-registerar-sidecar 
config: 
  environment_variables: { "ENV_AGENT_PATH": "b"}
  java_opts:
    system_properties:  { "SYSTEM_AGENT_PATH": "b"}
   # javaagents: [ "$SYSTEM_AGENT_PATH/pinpoint-bootstrap.jar" ]
   options: { "pinpoint.applicationName":"pinpoint.applicationName" }
